import { Offer } from "@prisma/client"


const IsActiveContainer = ({ offer }: { offer: Offer }) => (
  <div className="container mx-auto flex flex-col items-center justify-center min-h-screen p-4">
    {
    }
  </div>
);

export default IsActiveContainer;
